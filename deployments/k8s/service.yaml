apiVersion: v1
kind: Service
metadata:
  name: pinning-service-api
  namespace: pinning-service
  labels:
    app: pinning-service-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: pinning-service-api